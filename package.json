{
	"name": "zoom-warp-manager",
	"version": "1.0.0",
	"description": "Cloudflare Workers-based system to automatically update Zero Trust WARP profiles with Zoom IP ranges",
	"main": "src/index.ts",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"build": "wrangler deploy --dry-run"
	},
	"keywords": [
		"cloudflare-workers",
		"zero-trust",
		"warp",
		"zoom",
		"split-tunnel"
	],
	"author": "Your Name",
	"license": "MIT",
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.38.0"
	}
}
